// lib/supabase.ts
// Placeholder supabase client helper. Fill in with actual initialization
// using `@supabase/supabase-js` and environment variables when ready.

export function createSupabaseClient() {
  // TODO: Replace with real Supabase client initialization.
  // e.g. import { createClient } from '@supabase/supabase-js'
  // return createClient(process.env.SUPABASE_URL!, process.env.SUPABASE_ANON_KEY!)
  throw new Error("createSupabaseClient not implemented");
}

export type SupabaseClientPlaceholder = unknown;
